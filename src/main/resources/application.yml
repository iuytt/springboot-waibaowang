spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/waibao?serverTimezone=UTC&characterEncoding=utf8&useUnicode=true
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  type-aliases-package: com.chy.pojo
  mapper-locations: classpath:mapper/*.xml